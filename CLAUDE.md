プロジェクト概要

Google検索結果から、検索結果に表示される「相談窓口」を非表示にするためのGoogle Chrome/Firefox対応拡張機能。個人の閲覧環境を最適化し、不要な視覚的ノイズを減らすことを目的とする。
開発コマンド

本プロジェクトは標準的なWebExtensions APIを使用するため、特別なビルドステップは不要。

    拡張機能の読み込み: Chromeで chrome://extensions/ を開き、「デベロッパー モード」を有効にして「展開型の拡張機能を取り込む」からプロジェクトフォルダを選択。

    再読み込み: コードを変更した後は、拡張機能管理画面の更新アイコン（リロード）をクリックする。

プロジェクト構成
Plaintext

search-element-blocker/
├── manifest.json      # 拡張機能の設定（Manifest V3）
├── content.js         # 特定の要素を検知して非表示にするスクリプト
└── styles.css         # 非表示用のスタイルシート（display: noneなど）

実装ガイドライン

    Manifest バージョン: 常に Manifest V3 を使用する。

    パフォーマンス最適化: 要素が動的にロードされる場合に対応するため、MutationObserver を適切に使用するか、CSSインジェクションを優先してページのチラつき（レイアウトシフト）を防ぐ。

    セレクタの正確性: 他の重要な検索結果や必要なUIを巻き添えにしないよう、非表示にしたい要素のCSSセレクタはデベロッパーツールで確認し、厳密に指定する。

コード規約

    安全性と保守性の高いプレーンなJavaScript（ES6+）で記述する。

    脆弱性を避けるため innerHTML の使用は厳禁。要素の削除（element.remove()）または非表示クラスの適用（display: none !important）で行う。